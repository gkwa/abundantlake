version: 2
before:
  hooks:
  - go mod tidy
  - go generate ./...
builds:
- env:
  - CGO_ENABLED=0
  goos:
  - linux
  - windows
  - darwin
  main: main.go
  goarch:
  - amd64
  binary: abundantlake
  id: abundantlake
  ldflags:
  - -s -w
    -X "github.com/gkwa/abundantlake/version.Version={{ .Version }}"
    -X "github.com/gkwa/abundantlake/version.Date={{ .CommitDate }}"
    -X "github.com/gkwa/abundantlake/version.GoVersion={{ .Env.GOVERSION }}"
    -X "github.com/gkwa/abundantlake/version.ShortGitSHA={{ .ShortCommit }}"
    -X "github.com/gkwa/abundantlake/version.FullGitSHA={{ .FullCommit }}"
archives:
- format: tar.gz
  name_template: |-
    abundantlake_ {{- title .Os }}_ {{- if eq .Arch "amd64" }}x86_64
                               {{- else if eq .Arch "386" }}i386
                               {{- else }}
                               {{ .Arch }}
                               {{ end }}
                               {{- if .Arm }}v{{ .Arm }}{{ end }}
  format_overrides:
  - goos: windows
    format: zip
checksum:
  name_template: checksums.txt
snapshot:
  name_template: '{{ incpatch .Version }}-next'
changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'
brews:
- name: abundantlake
  directory: Formula
  homepage: https://github.com/gkwa/abundantlake
  repository:
    owner: gkwa
    name: homebrew-tools
    branch: main
scoops:
- name: abundantlake
  homepage: https://github.com/gkwa/abundantlake
  description: "Description of the abundantlake project"
  license: MIT
  repository:
    owner: gkwa
    name: scoop-bucket
    branch: master
